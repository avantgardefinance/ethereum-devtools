{
  "name": "@enzymefinance/ethereum-devtools",
  "private": true,
  "workspaces": [
    "packages/*",
    "config/*"
  ],
  "preconstruct": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "postinstall": "preconstruct dev && manypkg check && yarn compile",
    "compile": "hardhat compile",
    "coverage": "hardhat coverage",
    "dev": "preconstruct dev",
    "build": "preconstruct build",
    "watch": "preconstruct watch",
    "release": "yarn compile && yarn build && changeset publish",
    "typecheck": "manypkg exec yarn typecheck",
    "lint": "yarn lint:prettier && yarn lint:solhint",
    "lint:eslint": "eslint \"{*,**/*}.{js,jsx,ts,tsx}\"",
    "lint:prettier": "prettier --list-different \"{*,**/*}.{sol,json,js,jsx,ts,tsx}\"",
    "lint:solhint": "solhint --max-warnings 0 \"packages/*/contracts/{*,**/*}.sol\"",
    "format": "yarn format:prettier && yarn format:eslint",
    "format:prettier": "yarn lint:prettier --write",
    "format:eslint": "yarn lint:eslint --fix",
    "test": "TS_JEST_DISABLE_VER_CHECKER=true jest"
  },
  "dependencies": {
    "@changesets/cli": "^2.11.2",
    "@enzymefinance/babel-config": "^1.0.2",
    "@enzymefinance/eslint-config": "^1.0.1",
    "@manypkg/cli": "^0.17.0",
    "@preconstruct/cli": "^2.0.6",
    "eslint": "^7.21.0",
    "jest": "^26.6.1",
    "prettier": "^2.1.2",
    "solhint": "^3.3.1",
    "ts-jest": "^26.4.4",
    "typescript": "^4.2.3"
  },
  "prettier": "@enzymefinance/prettier-config-solidity",
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{json,md,yml}": "prettier --write",
    "*.{js,jsx,ts,tsx}": "eslint --fix",
    "*.sol": [
      "prettier --write",
      "solhint --fix"
    ]
  }
}
